<%= form_for(@daughter) do |f| %>
  <% if @daughter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@daughter.errors.count, "error") %> prohibited this daughter from being saved:</h2>

      <ul>
      <% @daughter.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :owner_id %><br>
    <%= f.number_field :owner_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
